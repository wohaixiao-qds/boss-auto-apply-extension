# BOSS 自动打招呼插件使用说明

## 项目简介

这是一个基于 Chrome Side Panel 的 BOSS 直聘自动打招呼扩展。插件在浏览器侧边栏运行，不覆盖或改变 BOSS 页面展示。

插件只读取 BOSS 页面和页面请求中的职位信息，发送时使用 BOSS 原生页面交互，不调用未公开的发送接口，也不绕过验证码、登录校验和频率限制。

## 技术栈

- TypeScript
- React
- WXT + Vite
- Radix Themes
- Lucide React
- Chrome Manifest V3
- Chrome Storage API
- Node.js 20 LTS

## 本地开发

```bash
npm install
npm run dev
```

生产构建：

```bash
npm run build
```

生成可分发压缩包：

```bash
npm run package
```

压缩包输出到 `release/boss-auto-greeting.zip`。

## 在 Chrome 中安装

1. 执行 `npm run build`。
2. 打开 Chrome 的 `chrome://extensions/`。
3. 开启右上角“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 选择项目下的 `.output/chrome-mv3` 文件夹。
6. 打开 BOSS 直聘并确认已经登录。
7. 点击 Chrome 工具栏中的扩展图标，打开侧边栏。

如果使用压缩包，先解压 `release/boss-auto-greeting.zip`，再在 Chrome 中选择解压后的文件夹。

## 使用流程

1. 在 BOSS 职位列表页手动完成城市、关键词、薪资等筛选。
2. 打开插件侧边栏，点击“扫描职位”。
3. 审核扫描结果，可移除不需要投递的职位。
4. 在投递设置中配置：
   - 是否排除外包岗位；
   - 每批最多投递数量；
   - 随机等待时间。
5. 点击“同意并开始”。
6. 任务执行期间可以暂停、继续或停止。
7. 任务完成后，侧边栏会显示本批次结果，并尝试发送 Chrome 系统通知。

## 职位状态

- 待投递：已扫描但尚未处理。
- 投递中：当前正在处理。
- 投递成功：已检测到 BOSS 原生页面的成功结果。
- 投递失败：页面异常、按钮不可用、登录失效或其他原因导致未完成。
- 已跳过：职位不满足投递条件或被过滤。

职位使用页面读取到的 `jobId` 关联，避免仅依赖公司名或职位名称造成误匹配。

## 注意事项

- 使用前请保持 BOSS 账号处于登录状态。
- 请不要在任务执行期间频繁刷新或手动切换 BOSS 页面。
- 遇到验证码、频率限制、登录失效或页面结构异常时，任务会自动暂停，请根据页面提示处理。
- 插件数据仅保存在浏览器本地，不连接外部 AI 或后端服务。
- Chrome 系统通知受操作系统通知权限、勿扰模式和浏览器设置影响。

## 测试与检查

```bash
npm run typecheck
npm test
npm run build
```

构建时 Radix Themes 依赖可能输出 `use client` 被忽略的提示。这是打包器对依赖模块指令的提示，不影响构建成功和插件运行。
